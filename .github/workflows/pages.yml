# Deploys the static export to GitHub Pages as a live QC preview of main.
# Not a production deployment. Actions are pinned to full SHAs per ENG-0005.
name: Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read # Checkout reads the selected commit; nothing else is needed.
  pages: write # Uploads the static export to the Pages environment.
  id-token: write # Requests the OIDC token that deploy-pages authenticates with.

concurrency:
  group: pages # Pages has a single environment; queue deploys instead of racing.
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and deploy static export
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build static export
        run: npm run build
      - name: Configure Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - name: Upload artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: out
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
